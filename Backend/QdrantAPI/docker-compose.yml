services:
  qdrant:
    build: .
    container_name: qdrant_api
    ports:
      - "3006:3006"
    volumes:
      - ./app:/app # Scripts
      - ../data:/app/data # Data
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
